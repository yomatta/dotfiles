# BACKGROUND README

From Omarchy...

Need to copy / recreate the script that will set the current background.

  - The 'current background' is actually a constant file named "background"
    - A script is used to to symlink a new backgorund into the constant "background" file location. This way, the backround setting in Hyprland can remain the same and the same, latest background will persist between restarts.
