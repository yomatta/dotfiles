#!/bin/bash

# take input for commit message
if [[ -z $1 ]]; then
  echo "Usage: gitup [git commit message]" >&2
  exit 1
fi

MESSAGE="$1"

# personal quick git updater. very basic.

git add .

git commit -m "$1"

git pull origin main

git push origin main
